# TASK P2-01 - OPTIONAL: fold your worksheet into SCHEMA.md
Run this ONLY if you changed any default in WORKSHEET.md sections W1/W2 (or
recorded decisions D3-D6). If you accepted all defaults, skip this session.

PRODUCES: an updated SCHEMA.md (replaces firewall-kit copy; version bump)
ATTACH THIS SESSION: CONTRACT.md, SCHEMA.md, WORKSHEET.md (filled), this card.

## PROMPT (type this instead of the standard BUILD prompt)
Read the attached files. Produce an updated SCHEMA.md that applies the
decisions recorded in WORKSHEET.md sections W1 and W2. You may change ONLY:
display labels, vocabulary value lists, severity bands, and the scoring
scale. NEVER rename entity keys or field names, and never remove entities or
relationships. Bump the version line. Output the entire updated SCHEMA.md as
one fenced code block.

## AFTER
1. Read the diff mentally: only labels/vocab/bands/scale changed? If it
   renamed a field or entity key, regenerate - do not accept.
2. Save over SCHEMA.md in the kit folder (back up the old one first).
3. From now on every session attaches this new SCHEMA.md.
